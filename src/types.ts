export type Memory = {
  id: string
  title: string
  caption: string
  date: string
  image: string
}

export type TimelineItem = {
  id: string
  title: string
  description: string
  date: string
}
